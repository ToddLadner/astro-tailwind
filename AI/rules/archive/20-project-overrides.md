# Project Overrides

Migration note: project-specific overrides should live in project folders or active project rules. Original guidance is preserved below.

Add project-specific constraints here only when they apply across the entire workspace.

Examples:

- Framework and version
- Required build commands
- Component library restrictions
- Naming conventions
- Files or directories agents must not edit
