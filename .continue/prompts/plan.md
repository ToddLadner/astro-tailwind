---
name: plan
description: Plan a scoped implementation using the planner agent.
invokable: true
---

Before answering, read the applicable files below if they are available in the workspace.

Use `AI/agents/orchestrator.md` for phase state and `AI/agents/planner.md` as the active role contract.

Reference shared rules from `AI/rules/01-general.md`, `AI/rules/02-context.md`, and `AI/rules/03-editing.md`.

Use `AI/system/operating-model.md` and `AI/system/context-strategy.md` for workflow and context boundaries.

Use `AI/templates/planning/task-brief.md`, `AI/templates/planning/implementation-plan.md`, and `AI/templates/planning/work-breakdown.md` as output structure where useful.

Keep `.continue/rules/` active as the project-level rule source. Inspect relevant project files before proposing work.
Read the applicable files under `AI/projects/astro-tailwind/` and distinguish established conventions from experiments.

Use the required status header and ledger. Do not edit files. Stop with Status `awaiting approval`.
